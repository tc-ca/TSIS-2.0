# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  vmImage: 'vs2017-win2016'

steps:

- task: PowerPlatformToolInstaller@0
  inputs:
    DefaultVersion: true

- task: PowerPlatformExportSolution@0
  inputs:
    authenticationType: 'PowerPlatformEnvironment'
    PowerPlatformEnvironment: 'TSIS 2.0 Dev'
    SolutionName: '$(SolutionName)'
    SolutionOutputFile: '$(Build.ArtifactStagingDirectory)\$(SolutionName).zip'
    ExportRelationshipRoles: true

- task: PowerPlatformUnpackSolution@0
  inputs:
    SolutionInputFile: '$(Build.ArtifactStagingDirectory)\$(SolutionName).zip'
    SolutionTargetFolder: '$(Build.SourcesDirectory)\$(SolutionName)'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      # echo commit all changes
              git remote add origin https://github.com/tc-ca/TSIS-2.0
              git config user.email "xiaoweiguang@gmail.com"
              git config user.name "tc-buildbot"
              git checkout development
              git add --all
              git commit -m "$env:COMMITMESSAGE"        
              echo push code to new repo 
              git push origin development